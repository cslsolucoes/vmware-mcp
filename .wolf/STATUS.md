# STATUS — MCPVMWare

> Single source of truth for resuming work. Read this FIRST when starting a session.
> Update this file at the end of every work phase so the next `/clear` resumes in 1 read.
> Last updated: 2026-08-20

---

## ✅ Done

<!-- Move items here from "🚀 Next phase" when finished. Group by area. -->

- **2026-08-20 12:44-13:30** — **Documentação "100%" (README 1030 + TOOLS.md completo) + novo modo `--all-url` (as 1030 tools num só processo) + deploy + config dos 3 vCenters.** (1) **Deploy** das 1030 (fecho das 9 ondas) verificado antes: binário p/ `D:\ServidorDataCenter`, smoke real ESXi → 907 no modo all. (2) **Docs:** `src/cmd/dumptools/main.go` faz dump offline do catálogo (`NewRegistry(ctx, nil, opts)` não derefencia o client → constrói Registry por modo e imprime name+description+inputSchema, o mesmo que `tools/list`) → `scripts/gen_tools_md.py` gera **`TOOLS.md`** (1030 tools, cada uma com descrição+sintaxe+tabela de params; 600 destrutivas marcadas). README 749→1030 com cobertura por produto (vSphere 907 [521 vcenter-only+386 vsphere-general] + VMC 95 + Workstation 28) + tabela por área + link. Commit `a1f1258`. (3) **`--all-url` = `ConnectionModeEverything`** (`src/tools/registry.go` + `main.go`): registra TODAS as classes (1030); primário vSphere (`--username/--password`) + secundários OPCIONAIS `--workstation-url`/`--cloud-aws-url` na mesma invocação; tools sem cliente conectado LISTAM mas retornam erro claro no call (CallTool já guardava wsClient/cloudClient nil). Exclusivo com `--vcenter/--vmware/--vmware-all-url`. `TestMode_Everything` valida o conjunto exato de 1030; gate 4/4 verde (tools/vmware/cloudaws/workstation); smoke do flag OK. Commit `1cf627b`. (4) **Deploy do binário `1030-allurl`** (hash `CC033E27…`, 27.5MB) p/ `D:\ServidorDataCenter\mcpvmware-mcp.exe` (nenhum processo rodava). (5) **`.mcp.json`:** VM01/02/03 `--vcenter-url`→`--all-url` (painel mostra 1030: 907 funcionais + 123 WS/VMC visíveis-mas-inertes sem backend); `mcpVmwareTest` mantém `--vmware-all-url` (907, ESXi). **Achado:** o "mojibake" (em-dash `â€"`) era artefato de leitura (Windows Python `open()` usa cp1252) — a fonte/live/TOOLS.md têm `—` correto; NÃO é bug. **Pendência do usuário:** reconectar os 4 servidores MCP no painel. **Limpeza sugerida (não feita):** `src/gen.exe` e `src/mcpvmware-mcp.exe` são binários stale tracked no git (~27MB cada) — candidatos a `git rm --cached` + gitignore.

- **2026-08-19 16:47-18:46** — **Análise de cobertura do projeto "Lab VMware vSphere 6.5" (e-book) adaptado ao ambiente REAL da CSL + verificação de hardware ao vivo + report + 4 memórias. NÃO alterou código do MCP (só leitura/análise/planejamento; binário e tools inalterados).** Feita a partir do workspace `D:\ServidorDataCenter`. Usuário perguntou se os MCPs cobrem o projeto do `E-BOOK-LAB-VMWARE-FINAL.pdf` (guia de 218/229 págs, John Costa: Workstation→3 ESXi→Win2016 AD/DNS→VCSA→cluster/vSwitches→VMs/templates/clones→**HP VSA iSCSI**→Storage vMotion/vMotion/HA/DRS/FT). E-book convertido p/ Markdown (`pdftotext -enc UTF-8`) → `D:\MCPVMWare\.workspace\E-BOOK-LAB-VMWARE-FINAL.md` (10 tarefas viradas em headings). **Cobertura (evidência: grep em `src/**/*.go`, não teoria):** a camada de administração vSphere está coberta pelas ~749 tools — host (NTP/serviços/firewall/rede), rede (vSwitch/vDS/VLAN/portgroup/vmkernel + `host_nic_select_vnic`), storage (`host_iscsi_*` + `host_iscsi_portbinding` + multipath + VMFS + `storage_drs`), VM/clone/template/customize, cluster (`folder_create_cluster` + `compute_resource_reconfigure` com **HA+DRS** via `ClusterConfigSpecEx`, com o teste `reconfigure_cluster_enables_drs_and_ha`), **vMotion** (`vm_migrate`), **Storage vMotion** (`vm_relocate`), VAMI. **🔴 Única lacuna de código: Fault Tolerance** — ZERO tools `CreateSecondary`/`TurnOnFT` (`grep -i fault` → só comentários); fechável em `generated_vm_ft.go` (~4-6 tools raw-`methods.*`, mesmo padrão do fechamento iSCSI). Fora do alcance de QUALQUER MCP (por design): bootstrap (mídia/Workstation/instalar SO/deploy do VCSA) e config no SO convidado (AD/DNS — sem GuestOperations). **Adaptação HP VSA → TrueNAS:** T9.1 (storage) deixa de ser fora-de-escopo e passa a coberta pelo MCP TrueNAS (`pool_create`→`create_dataset` zvol→`iscsi_global/portal/initiator/auth/extent/target/targetextent`→`service_control`); fluxo integrado dos 2 MCPs documentado no report. **Verificação de hardware ao vivo** (autorizada pelo usuário; `vmware_host_info`, somente leitura, via mcpVmwareTest+VM01+VM02): **VM01** `vm-csl01.csltech.local` Dell R710, 2×Xeon **X5670** (Westmere) 12c/128GB, ESXi 6.7u3, 10.222.0.251; **VM02** `vm-csl02.csltech.local` R710, 2×**X5690** (Westmere) 12c/128GB, 6.7u3, 10.222.0.252; **VM03** `localhost.cslsolucoes.com.br` ⚠️ R720, 2×**E5-2697 v2** (Ivy Bridge) 24c/256GB, 7.0u3, 10.222.0.253 (/10.100.2.58 manut.). Total **48 cores / 512 GB**; cada host 2×SFP+ 10GbE + 2×RJ45 1GbE. **Decisões do owner:** (1) storage = **TrueNAS**; (2) **vCenter 8.0.2 → 7.0u3, instalação NOVA (greenfield)** — vSphere 8 não gerencia ESXi 6.7; o 7.0u3 gerencia 6.7+7.0 ⇒ os 3 hosts no mesmo vCenter sem tocar neles; (3) **manter** VM01/02 em 6.7u3 (Westmere/R710 fora do HCL do ESXi 7.0 — upgrade não passaria); (4) **EVC = Intel Westmere** (cluster misto; VMs perdem AVX); (5) rede **2 switches SFP+ 10GbE**, cada host 1 porta por switch (A/B), tráfego por VLAN (mgmt/vMotion/VM/FT em teaming; **iSCSI-A/B em MPIO**, nunca LACP), MTU 9000, TrueNAS com 2 portais; (6) ordem greenfield: VCSA em datastore local do VM03 → add 3 hosts → vDS+VLANs → iSCSI+VMFS → **storage vMotion do VCSA** p/ o TrueNAS → HA+DRS+EVC. **Pendências:** confirmar se os 2 switches são **stack/MLAG ou independentes**; saneamento pré-cluster (renomear VM03 `localhost`→`vm-csl03`; unificar DNS `csltech.local`×AD `cslsolucoes.com.br` + PTR reverso). **Artefatos:** report vivo `D:\MCPVMWare\.workspace\reports\MCPVMWare2026-08-19-165503-cobertura-ebook-lab-vmware.report.md` (com §11 do ambiente real + changelog); e-book em MD; **4 memórias** em `D:\Users\claiton.linhares\.claude\projects\d--ServidorDataCenter\memory\` (`datacenter-esxi-hosts-hardware`, `lab-vmware-truenas-projeto`, `mcp-vmware-cobertura-ft-gap`, `lab-vmware-report-artefatos`) + índice `MEMORY.md`. **Próximo dev no MCP: implementar Fault Tolerance** — único gap de código para o lab.

- **2026-08-19 15:42-16:12** — **Lacuna iSCSI fechada: 15 tools novas (initiator config + multipath) — vsphere-general 251→266, total 734→749.** Usuário perguntou "não há todas as ferramentas para tratar iSCSI?" — verificado por evidência: ZERO ocorrências de `iscsi` em `src/**/*.go`. **Causa-raiz achada:** o "100%" das 611 tools vSphere é 100% dos wrappers `object.*`; `object.HostStorageSystem` só tem 13 métodos, nenhum iSCSI — a família `*InternetScsi*` + `EnableMultipathPath`/`DisableMultipathPath`/`SetMultipathLunPolicy` só existe no MO SOAP cru (72 refs de `InternetScsi` em `vim25/methods/methods.go`), sem wrapper `object.*`, então o gerador (que caminhou sobre `object/`) nunca as viu. **Implementado** `src/tools/generated_host_iscsi.go` — 12 tools iSCSI (`vmware_host_iscsi_*`: software adapter enable, IQN/alias, send/static targets add/remove, CHAP auth, digest, discovery, IP, advanced options) + 3 multipath (`vmware_host_storage_{enable,disable}_multipath_path`, `_set_multipath_lun_policy`), todas tier2, todas `modeVSphereGeneral` (iSCSI é config de host ESXi — funciona em standalone). Cada handler chama `methods.Xxx(ctx, client.Client.Client, &types.Xxx{This: ss.Reference(), ...})` direto (padrão raw-`methods.*` de `generated_host_security.go`), `ss` do helper `hostStorageSystem()` existente; structs complexas (targets, CHAP/digest/discovery/IP props, target set) via `decodeJSONArg` (mesmo padrão dos specs de `generated_host_storage.go`). Inventário dos métodos/tipos vim25 levantado por subagent Sonnet (203k tokens). Editados `registry.go` (+1 `withClass`) e `mode_test.go` (`vsphereGeneralTools` +15, senão os 6 `TestMode_*` quebram por contagem). `generated_host_iscsi_test.go` — Registration(15) + Validation(14 casos de arg faltando, pré-round-trip) + GateAndConfirm + ReachesServer(15). **Verificação:** `go build ./...`+`go vet` limpos; suite COMPLETA `go test ./...` verde (tools/cloudaws/vmware/workstation ok, ~83s); os 15 ReachesServer provam empiricamente a fiação — cada um retorna `HostStorageSystem:hoststoragesystem-6 does not implement: <MétodoCerto>` (método SOAP e MoRef corretos chegam ao vcsim; só o simulador não implementa — validação comportamental fica pro ESXi real). Binário recompilado em `d:/MCPVMWare/mcpvmware-mcp.exe` (25.4MB). **Deploy CONCLUÍDO 16:12-16:29** (usuário autorizou "deploy para todos os 4"): matados os 9 processos que travavam o `.exe` no Windows, `D:/ServidorDataCenter/mcpvmware-mcp.exe` substituído (hash SHA256 idêntico ao build novo, cópia íntegra), e **smoke real do binário de produção** contra o ESXi 10.100.2.58 (`--vmware-all-url`) confirmou **626 tools (611+15 iSCSI)** todas presentes + `vmware_about` real OK (ESXi 7.0.3 build-21930508). **Resta:** (a) usuário reconectar os 4 servidores no painel MCP (os 9 processos foram mortos p/ destravar; nenhum ativo agora); (b) commit — decidido NÃO commitar agora (base 749 tools segue uncommitted por política).**
- **2026-08-12 18:40-19:15** — **Fase 10 do plano de cobertura completa executada: VMware Cloud on AWS (VMC), 95 tools novas de verdade — arquitectura de 3º cliente, FECHA O PLANO INTEIRO A 100% (734 tools).** Continuação de "ok, finalizar 100%" — esta é a ÚLTIMA fase do plano. Terceiro produto distinto (sem `govmomi`), auth CSP token-exchange (refresh_token→access_token via `POST .../csp/gateway/am/api/auth/api-tokens/authorize`) — construí `src/cloudaws/client.go` (cache+renovação automática+1 retry em 401), verificado com 5 testes próprios antes de delegar. Extraí e curei 95 rotas reais de 99 brutas (1 exclusão de plumbing interno, 3 duplicados). **Achado de risco financeiro real, tratado com cautela redobrada**: toda escrita no domínio SDDCs ficou Tier1 por padrão (mais cauteloso que a convenção normal do resto do projecto), dado que um SDDC na AWS custa dinheiro por hora. 4 grupos paralelos: orgs(29) + sddcs(23, o mais sensível) + networking-core(19) + networking-edge(24) — **zero colisões de build entre os 4 grupos**, melhor resultado de toda a sessão. Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas, **2 smokes reais** (regressão vSphere 611 tools intactas + CloudAWS novo 95 tools, gate tier1 negando `sddc_create` — deliberadamente sem chamar nenhum tool sem gate, pra nunca tocar os servidores de produção reais da VMware num smoke não-assistido). Report formal: `.workspace/reports/MCPVMWare2026-08-12-191500-fase10-cloudaws-codegen.report.md`. **Plano completo (Fases 0-10) fechado.**
- **2026-08-12 18:15-18:40** — **Fase 9 do plano de cobertura completa executada: VMware Workstation Pro (`vmrest`), 28 tools novas de verdade — arquitectura de cliente duplo construída de raiz (639 total).** Continuação de "ok, finalizar 100%". Produto distinto de vSphere, sem `govmomi` — construí `src/workstation/client.go` (HTTP+Basic Auth, media type `vnd.vmware.vmw.rest-v1+json`, `ErrorModel` capitalizado, `DoRawBody` pro corpo cru de `PUT /vms/{id}/power`), verificado com 6 testes próprios antes de delegar. **Achado arquitectural crítico resolvido pelo orquestrador**: `Tool.Handler`/`Registry.client` estavam hardcoded a `*vmware.Client` — adicionado um SEGUNDO campo `Tool.WSHandler`+`Registry.wsClient`+`RegistryOptions.WorkstationClient` (opcional, não parâmetro novo) — zero ficheiros de teste existentes mudaram. 2 grupos paralelos: workstation-vm(11, corrigiu 3 divergências reais spec-opcional→schema-obrigatório) + workstation-network(17, não 18 — "Host Networks" só tem 7 rotas reais). Achado de design na integração: `ConnectionModeAll` já incluía `modeWorkstation` desde 10/08 mas `--vmware-all-url` só tem 1 cliente — corrigido pra vSphere-only até o "2 clientes vivos" (já sinalizado desde 10/08) ser decidido. Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas, **2 smokes reais** (regressão vSphere 611 tools intactas + Workstation novo 28 tools contra fixture httptest, incl. gate destructivo negando `power_set` sem `--allow-destructive`). Report formal: `.workspace/reports/MCPVMWare2026-08-12-184000-fase9-workstation-codegen.report.md`.
- **2026-08-12 17:35-18:10** — **Fase 8b do plano de cobertura completa executada: VAMI via parse de collection Postman (técnica NOVA), 116 tools novas de verdade — fecha a Fase 8 inteira (611 total).** Continuação de "ok, finalizar 100%". Primeira vez neste projecto sem AST sobre Go — parse estruturado (Python) da collection `.workspace/vSphere Automation REST Resources for appliance.postman_collection.json` (132 rotas). **Achado importante de dedup**: os 15 métodos Go da Fase 8a (`vapi/appliance/*`) usam paths `/api/appliance/...` (modernos), enquanto a collection cataloga só `/rest/appliance/...` (legacy) — NÃO são duplicados literais, são 2 gerações de API distintas pra mesma capacidade. Só 12 rotas excluídas de facto (10 já cobertas pela Fase 4, 2 de plumbing de sessão). 4 grupos paralelos: recovery-update(31, tier1 em restore/update-install), network-health-system(19, criou `applianceRequest` generalizando o `applianceGet` da Fase 4), techpreview-network(27, confirmou por grep que "techpreview" não tem NENHUM SDK Go em lado nenhum), services-accounts-vmon-access-shutdown(39, resolveu 1 colisão real de símbolo com outro grupo). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas (~80-87s cada), smoke real do binário via stdio confirmando 611 tools + 2 chamadas VAMI reais devolvendo 404 limpo esperado (vcsim sem rotas `/rest/appliance/*`). Report formal: `.workspace/reports/MCPVMWare2026-08-12-181500-fase8b-vami-postman-codegen.report.md`.
- **2026-08-12 16:34-17:35** — **Fase 8a do plano de cobertura completa executada: VAPI/REST via wrappers Go (AST), 202 tools novas de verdade em 2 levas (495 total).** Continuação de "ok, finalizar 100%". **Achado de infra crítico**: vcsim tem um simulador REST/VAPI separado (`vapi/simulator`, nunca usado neste projecto) que só arranca com blank-import + `RegisterEndpoints=true` — confirmado com spike próprio, aplicado uma vez em `testhelpers_test.go`. Leva 1 (105 tools, domínios COM suporte real no vcsim): library-core(27)/library-sessions(26)/library-misc(15)/tags-vcenter(37) — Content Library+Tags+templates vCenter, 4 bugs reais do vcsim achados. Leva 2 (97 tools, domínios SEM suporte, "vcsim gap" confirmado por evidência dupla): namespace-core(22)/namespace-services(21)/cluster-settings-crypto-tasks(29)/vm-dataset-appliance-small(25) — achou drift real `referencia/govmomi` vs `go.mod` pinado (`bug-012`, apanhado pelo gate de build), 1 bug no próprio govmomi vendorizado, 1 caso de sub-classificação (`authentication.Issue`). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas (~78s cada), smoke real do binário via stdio confirmando 495 tools. Report formal: `.workspace/reports/MCPVMWare2026-08-12-173500-fase8a-vapi-rest-codegen.report.md`.
- **2026-08-12 15:05-15:40** — **Fase 7 do plano de cobertura completa executada: domínio Gestão/Operações, 57 tools novas de verdade (293 total).** Usuário: "ok, finalizar 100%" (continuação directa, sem pausa). `option_manager.go` já coberto pela Fase 1, `namespace_manager.go` já coberto pela Fase 4 (só `DatastoreNamespaceManager`, hipótese anterior de Namespace Kubernetes-Supervisor descartada — isso vive em `vapi/namespace`, Fase 8) — ambos excluídos sem re-trabalho. **3 bugs reais de classificação de MODO achados antes de gerar** (mesmo método da Fase 4): `extension_manager.go`/`custom_fields_manager.go`/`customization_spec_manager.go` estavam `vsphere-general`, deviam ser `vcenter-only` (confirmado por `ServiceContent` nil em ESX/populado em VPX) — corrigido `gen/main.go`, regenerado. Curadoria fundiu métodos redundantes (4 variantes de `Task.Wait*`→1 tool; `DiagnosticLog.Copy`+`Seek`→1 tool) reduzindo 62 métodos candidatos pra 57 tools reais. 3 grupos paralelos (Sonnet): **task/diagnostic/search** (18, confirmou `DiagnosticManager` com zero simulação no vcsim); **authorization/custom-fields** (20, criou `resolveEntityRef(s)` via `SearchIndex` — capacidade nova reusada pelo grupo C — confirmou que a API interna `urn:internalvim25` nem é reconhecida pelo vcsim); **extension/customization-spec/tenant** (19, corrigiu um erro factual meu próprio no brief, achou e sinalizou um bug real fora do seu escopo — `bug-011`, comentário falso em `generated_vm_provisioning.go` da Fase 2 sobre decode polimórfico funcionar quando não funciona). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas (~52s cada), smoke real do binário via stdio confirmando 293 tools + chamadas reais bem-sucedidas. Report formal: `.workspace/reports/MCPVMWare2026-08-12-154000-fase7-management-codegen.report.md`.
- **2026-08-12 14:36-15:05** — **Fase 6 do plano de cobertura completa executada: domínio Inventário/Organização, 40 tools novas de verdade (236 total).** Usuário: "ok, finalizar 100%" — autorização para avançar por todas as fases restantes (6-10) sem pausar pra aprovação entre elas. 40 métodos candidatos de `Folder`(11)/`Datacenter`(3)/`ComputeResource`(5)/`ClusterComputeResource`(4)/`ResourcePool`(7)/`VirtualApp`(6)/`EnvironmentBrowser`(4) revistos antes de gerar: reversão deliberada do padrão "skip zero-arg accessor sem consumidor" pra `ComputeResource.EnvironmentBrowser()` (4 consumidores reais nesta mesma fase); 2 métodos dry-run corrigidos tier2→sem-tier (`ClusterComputeResource.PlaceVm`, `Folder.PlaceVmsXCluster`); vários zero-arg accessors corrigidos (mesmo padrão desde a Fase 0). 3 grupos paralelos (Sonnet): **folder/datacenter** (14, achou+corrigiu nil-deref real em `Folder.CreateVM` sem `resource_pool`, e um `RegisterVM` com `.nvram` faltando no vcsim — não é bug da tool); **compute/cluster/environment** (13, corrigiu tipo — `types.ComputeResourceConfigSpecEx` não existe, usado `types.ClusterConfigSpecEx`); **resourcepool/vapp** (13, achou que `ResourcePool.CreateVApp`/`CreateChildVM` são desabilitados por vSphere de verdade em ESXi standalone — não é gap do vcsim — e corrigiu `ImportVApp` pra `types.VirtualMachineImportSpec`). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas (~44s cada), smoke real do binário via stdio confirmando 236 tools + chamadas reais bem-sucedidas. Report formal: `.workspace/reports/MCPVMWare2026-08-12-150500-fase6-inventory-codegen.report.md`.
- **2026-08-11 15:25-15:45** — **Fase 5 do plano de cobertura completa executada: domínio Rede, 7 tools novas de verdade (196 total).** Usuário: "ok, prossiga". Escrito directamente por mim (sem subagents — domínio pequeno demais pra justificar o overhead de coordenação, mesma decisão da Fase 2 "snapshot"). 32 métodos candidatos revistos: `HostNetworkSystem` (21) já coberto na Fase 3, excluído por já feito; 4 métodos `EthernetCardBackingInfo()` excluídos (sem tool de criação de NIC pra encadear); `OpaqueNetwork.Summary()` corrigido tier2→sem-tier. **1 bug no meu próprio processo de teste**: tentei criar uma fixture "DVS0" que colidia com a que `simulator.VPX()` já cria por defeito (efeito colateral de `Model.Portgroup: 1`) — corrigido removendo a criação desnecessária. **Achado estrutural**: as 7 tools misturavam modo vcenter-only (6, DVS/DVPG) e vsphere-general (1, OpaqueNetwork) numa função só — `withClass` só aplica 1 modo por chamada, dividido em 2 funções (mesmo padrão da Fase 2). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas, smoke real do binário via stdio confirmando 196 tools. Report formal: `.workspace/reports/MCPVMWare2026-08-11-154500-fase5-network-codegen.report.md`.
- **2026-08-11 14:05-14:45** — **Fase 4 do plano de cobertura completa executada: domínio Storage/Datastore, 43 tools novas de verdade (189 total).** Usuário: "ok, prossiga". **Achado real de classificação antes de gerar:** `StorageResourceManager`/`DatastoreNamespaceManager` estavam erradamente `vcenter-only` desde a Fase 0 — verifiquei os construtores contra `ServiceContent` de ESX/VPX, ambos não-nulos nos dois, corrigido `gen/main.go` e re-rodado o gerador. 5 métodos de `Datastore` excluídos (não mapeiam pra JSON: `Upload`/`Download`/`HostContext`/`WithProgress`/`FindInventoryPath`/`Browser` — retornam `io.Reader`/`context.Context` ou só mutam o próprio receiver). 4 grupos paralelos (Sonnet): **datastore browser/namespace** (12, achou+corrigiu 1 crash real do vcsim — `search_spec` nil causava panic dentro do simulador, `bug-010`); **file managers** (11, corrigiu um construtor que eu tinha inventado errado, e sinalizou uma imprecisão factual no comentário do grupo "virtual disk" sobre os 2 tools `delete_virtual_disk` — corrigido na integração); **storage DRS** (9, corrigiu 1 tier sozinho, achou limitação arquitectural ampla: `vim25/types` não tem `UnmarshalJSON` customizado em lado nenhum); **virtual disk** (11, achou que `SetVirtualDiskUuid` do vcsim é um stub permanente que nunca persiste, provado com teste). Verificação: `go clean -testcache && go test ./... -count=1` verde 2x seguidas (33-34s), smoke real do binário via stdio confirmando 189 tools + 4 chamadas reais bem-sucedidas. Report formal: `.workspace/reports/MCPVMWare2026-08-11-144500-fase4-storage-codegen.report.md`.
- **2026-08-11 11:52-12:15** — **Fase 3 do plano de cobertura completa executada: domínio Host inteiro, 70 tools novas de verdade (146 total).** Usuário: "ok, prossiga". Revisão prévia excluiu os 12 métodos de `HostConfigManager` (só construtores internos, mesmo padrão de `generated_option.go`) e verificou suporte real do vcsim por nome de método SOAP (não por nome do tipo Go — corrigiu um erro meu a meio sobre `HostAccountManager`) antes de delegar. 4 grupos paralelos (Sonnet): **storage** (20, corrigiu 1 tier por conta própria — `ComputeDiskPartitionInfo` é dry-run, não tier2); **network** (21, achou e corrigiu 1 bug real — `query_network_hint` devolvia `null` em vez de `[]`, `bug-009`); **security** (14, investigação real: `HostAccountManager` simulado em `ESX()` mas não em `VPX()`, o oposto do padrão habitual, provado com teste; achou também um bug no vcsim vendorizado — `UpdateUser` devolve tipo de resposta errado, não corrigido por ser `referencia/` só-leitura); **misc** (15, corrigiu uma suposição errada minha sobre resolução local dos managers). **Achado de processo importante:** apliquei preventivamente a lição da Fase 2 (nunca aceitar relatório vago) dando instruções explícitas de "nunca terminar vago"/"matar teste que arrisque ficar preso" — **nenhum dos 4 agents ficou preso desta vez**, todos voltaram com `go test` real colado. Verificação: `go clean -testcache && go test ./... -count=1` 100% verde (28.9s), smoke real do binário via stdio contra VPX confirmando 146 tools + 3 chamadas reais bem-sucedidas. Report formal: `.workspace/reports/MCPVMWare2026-08-11-121500-fase3-host-codegen.report.md`.
- **2026-08-10 20:36-21:15** — **Fase 2 do plano de cobertura completa executada: domínio VM inteiro, 41 tools novas de verdade (76 total).** Usuário: "ok, prossiga". Revisão humana (por mim) dos 53 métodos candidatos ANTES de gerar — excluídos 3 duplicados reais de `vm.go` (dedup exacto da Fase 0 não apanhou por ordem de palavra diferente) + 2 adiados (`*WithProfile`, complexidade); `UnmountToolsInstaller` corrigido tier1→tier2. 48 restantes, divididos em 4 grupos por ficheiro (evita colisão de agents paralelos): **snapshot** (4, escrito por mim — achou que `FindSnapshot` nunca devolve `(nil,nil)`, sempre falta no "não encontrado", corrigido pra `found:false`); **device** (5, subagent Sonnet, MVP só disco/CD-ROM — achou um crash **dentro do próprio vcsim** em `AttachDisk`/`ESX()`, bug do simulador não nosso, contornado testando só contra VPX); **provisioning** (11, subagent Sonnet — achou ANTES de gerar que `VmProvisioningChecker`/`VmCompatibilityChecker` **crashavam** [nil-pointer] contra ESXi standalone, corrigido com guarda explícita + os 3 checkers movidos pra `vcenter-only`, achado de mode errado da Fase 0); **lifecycle** (25, subagent Sonnet, relatório final veio vago porque o próprio teste dele tinha ficado **preso 10 minutos reais** — só descoberto correndo eu mesmo `go test` de novo em vez de aceitar o relatório incompleto). **3 bugs reais atrás do hang, achados e corrigidos por mim depois de desbloquear:** (1) `wait_for_ip/wait_for_net_ip/wait_for_power_state` sem timeout nenhum — comentário dizia "confia no timeout do MCP" mas `grep` provou que não existe timeout nenhum em lado nenhum; corrigido com `timeout_seconds`+`context.WithTimeout`; (2) `WaitForNetIP` genuinamente não-testável contra vcsim (lê campo diferente do que o fixture de teste do vcsim escreve) — documentado, não escondido; (3) `vmware_vm_host_system`/`resource_pool` devolviam `""` — objectos construídos fora do Finder têm `InventoryPath` sempre vazio, corrigido com `find.InventoryPath`. **Achado de segurança adicional aplicado a TODO o Registry:** `CallTool` ganhou `recover()` — não existia nenhum antes, protege as ~480 tools ainda por vir de um bug local derrubar o processo inteiro. **Verificação final:** `go clean -testcache && go test ./... -count=1` 100% verde (não incremental); smoke real do binário compilado via stdio JSON-RPC contra `simulator.VPX()` — 76 tools em `tools/list`, 2 `tools/call` reais bem-sucedidos.
- **2026-08-10 20:05-20:25** — **Fase 1 do plano de cobertura completa executada: piloto de codegen ponta-a-ponta, `OptionManager` (2 tools).** Escolhido em vez de `HostServiceSystem` (a outra opção do plano) por evidência — `grep` confirmou que vcsim **não simula** `HostServiceSystem`, mas simula `OptionManager` de verdade (usado em `host_system.go`/`container_host_system.go`). Novo `src/tools/generated_option.go` (`vmware_host_option_query`/`vmware_host_option_update`, renomeados do que o gerador propôs — "option" sozinho era ambíguo — e escopados por host, não global, decisão de curadoria documentada no plano) + `src/tools/generated_option_test.go` (3 testes: ciclo funcional vcsim real, gate+confirm negando antes de tocar o simulador, rejeição de 4 casos de input malformado) + wiring em `registry.go`+`mode_test.go`. **1 bug real achado e corrigido ao correr de verdade** (`.wolf/buglog.json` bug-005): `OptionManager.Query` falta com `InvalidName` quando o filtro não bate nada — 1ª tentativa de detecção usou o par errado (`soap.IsVimFault`, que é pra faults de Task) e continuou falhando silenciosamente após rebuild; corrigido lendo `vim25/soap/client.go` de verdade (`soap.IsSoapFault`+`ToSoapFault().VimFault()` é o par certo pra chamada SOAP directa) — padrão registado em `.wolf/cerebrum.md` porque vai reaparecer nas Fases 2-8. `go build/vet/gofmt/test` 100% limpos, **31 tools registadas** (29+2). Smoke do binário real via subprocess **não repetido** desta vez (main.go não mudou, padrão já provado antes) — decisão documentada, não silenciosa. **Nenhuma tool a mais gerada — aguarda revisão/aprovação do usuário do piloto antes da Fase 2** (critério do próprio plano).
- **2026-08-10 19:48** — **Revisão humana da Fase 0 feita (a pedido do usuário, "faça você" — eu reviso em vez dele ler as 536 linhas).** Script Node sobre `gen/classification.json` (sistemático, não leitura linear do `.md`), achados verificados por evidência (grep no `gen/main.go`, `ls` real em `referencia/govmomi/object/`, comparação com a fonte real). **9 achados, detalhados no plano `.workspace/plans/MCPVMWare2026-08-10-175300-plano-cobertura-completa-api-codegen.plan.md` §"Revisão humana da Fase 0 executada".** Resumo por prioridade — **ALTA (recomendo corrigir antes do piloto):** (1) DVS+Storage DRS deviam ser `vcenter-only`, estão `vsphere-general` (ficheiros exactos identificados); (2) **dedup contra as 29 tools hand-written não existe** — a nota anterior no plano dizendo que existia estava ERRADA (corrigida), 8 colisões reais de nome achadas; (3) 1 colisão de nome interna ao próprio relatório (`Network`/`OpaqueNetwork.EthernetCardBackingInfo` → mesmo `proposed_tool`); (4) 2 métodos com verbo tier1 fora do prefixo escaparam pro tier2 (`KmsProviderDelete`, `ForceDeleteLibrary` — este último claramente tier1, delete forçado de library inteira). **MÉDIA:** `UnmountToolsInstaller` tier1 por colisão de keyword (é trivial, não perigoso); 3 casos tier1 questionáveis pela própria definição do projecto (`Disconnect`/`DetachDisk`/`Unregister` são reversíveis); ~40 dos 68 "tier2 zero-param" parecem accessors puros (candidatos a downgrade, mas SEM regra automática — outros ~19 zero-param são acções reais tipo `PowerOn`/`Suspend`/`MarkAsTemplate`); `UpgradeVM` talvez devesse ser tier1 (irreversível na prática). **BAIXA:** renderer de tipos garbled em 4/536 assinaturas (`func()`/`map` viram `*ast.FuncType`/`*ast.MapType`) — cosmético agora, mas precisa fix antes da Fase 1 gerar código de verdade. **Verificação negativa:** os 139 sem-tier (read-only) foram checados um a um por palavra suspeita — nenhum é mutante de verdade, balde limpo. **Nenhum `tools/*.go` gerado — aguarda decisão do usuário: corrigir o gerador primeiro, ou seguir pro piloto (Fase 1) como está.**
- **2026-08-10 19:35-20:00** — **Fase 0 do plano de cobertura completa executada (parcial): gerador construído e rodado de verdade.** `src/gen/main.go` (parser AST, só stdlib) sobre `object/`+`vapi/*`. **Achou e corrigiu 1 bug nele mesmo**: 1ª versão não achava nada em `vapi/appliance` (tratava como pacote flat, mas o conteúdo real está em subpastas — `appliance/{access,logging,networking,shutdown}` etc.) — corrigido com caminhada recursiva. **Números reais (substituem a estimativa de `grep` no plano): 318 métodos em `object/` (não 448) + 218 em `vapi/*` (não 310) = 536 total confirmados** (Tier1=56, Tier2=341 fail-safe, sem tier=139). Spot-check de sanidade bate com as classificações já feitas à mão (`Destroy`/`RemoveSnapshot`/`RevertToSnapshot`→tier1, `Suspend`→tier2). Escreveu `src/gen/classification.json` + `src/gen/CLASSIFICATION_REPORT.md` (536 linhas) — **nenhum `tools/*.go` gerado ainda**, aguarda revisão humana do relatório (gate explícito do plano).
- **2026-08-10 19:35** — Consultado o portal Broadcom (`developer.broadcom.com/xapis/vsphere-automation-api/latest/`, `reference_broadcom_developer_portal`) via `WebFetch`, a pedido do usuário, pra ver se complementa a coleção Postman de VAMI já vendorizada. **Confirma** que a doc SOAP/REST "clássica" sumida do PDF 9.1 migrou pra este portal web (cobre até vSphere 9.1). **Achado que revisa a estimativa do gap:** categoria Appliance tem 57 subcategorias, portal estima 150-200+ operações — mais que as 132 rotas da nossa coleção Postman (provavelmente de versão mais antiga) — o "~117 gap" do plano é **piso, não tecto**. Achado um índice de operações real (`.../operation-index/`) mas não extraível de uma vez (WebFetch trunca); sem spec OpenAPI/Swagger pra download. Adicionado ao plano como nota, não bloqueante.
- **2026-08-10 19:11-19:30** — **Modos de conexão implementados** (5 flags: `--vcenter-url`/`--vmware-url`/`--workstation-url`/`--vmware-all-url`/`--cloud-aws-url`, mutuamente exclusivas), a pedido do usuário ("execute o plano" — a parte concreta e imediatamente executável do plano de cobertura completa, não o gerador de código de ~875 tools, que continua atrás do gate de revisão da Fase 0):
  - `tools/registry.go`: `ConnectionMode` (5 constantes) + `toolMode` (4 classes: `vcenter-only`/`vsphere-general`/`workstation`/`cloud-aws`) + `connectionModeAllows(mode, class)`. Design que evitou tocar nos ~36 call-sites de `r.register`/`r.registerDestructive` existentes: `withClass(class, fn)` define `r.currentClass` transientemente durante cada `registerXTools`, e `register()` consulta isso — só `registerTools()` mudou, não os ficheiros individuais. Zero-value de `ConnectionMode` (`""`) = sem filtro, garante que os 6 arquivos de teste já existentes (que constroem `RegistryOptions{}` sem mexer em modo) continuam vendo todas as tools sem alteração nenhuma.
  - `mcpvmware-mcp/main.go`: `resolveConnectionMode()` valida exactamente 1 das 5 flags; `--workstation-url`/`--cloud-aws-url` reconhecidas mas recusam arrancar ("not implemented yet", Fases 9/10 do plano de cobertura não implementadas) — falhar alto é melhor que ligar ao protocolo errado silenciosamente.
  - **Retrofit das 29 tools existentes:** appliance (4) → `vcenter-only`; as outras 25 → `vsphere-general`. Nenhuma tool workstation/cloud-aws existe ainda.
  - `tools/mode_test.go` — 6 testes, filtragem provada por **igualdade de conjunto** (não "contém"): unrestricted/vcenter/all = 29 tools; vmware(ESXi) = 25 (sem as 4 de appliance, checado por nome individual); workstation/cloud-aws = 0.
  - **Smoke end-to-end extra do binário real** (subprocess dirigindo `mcpvmware-mcp.exe` via stdio contra vcsim): `--vcenter-url` → 29 tools com appliance; `--vmware-url` → 25 sem appliance; as duas flags juntas → falha com a mensagem de exclusividade mútua; `--workstation-url` sozinho → falha com "not implemented yet". Todos os 4 cenários confirmados batendo exactamente com o desenho.
  - `go build/vet/gofmt/test` limpos (33 testes nos 2 pacotes), binário recompilado. **O resto do plano de cobertura completa (gerador de código pra ~875 tools govmomi, Fase 9 Workstation, Fase 10 VMC on AWS) continua não-implementado** — "execute o plano" nesta rodada cobriu só a parte de infraestrutura de modos de conexão, que era a peça concreta e imediatamente accionável sem precisar do gate de revisão da Fase 0.
- **2026-08-09** — Mapeada a superfície de API disponível para o futuro MCP layer: SDK Go vendorizado em `src/` (govmomi, SOAP/vim25) + 7 coleções Postman em `.workspace/` (REST/VAPI). Ver tabela em "Active architecture" abaixo. Análise feita por evidência (leitura de go.mod/README/doc.go/client.go do govmomi + leitura direta das 7 coleções .postman_collection.json), não por suposição.
- **2026-08-09** — **(achado em sessão anterior, não registado até agora — corrigido)** Scaffold Go do servidor MCP já existe na raiz e compila limpo (`go build`/`go vet`/`gofmt` sem apontamentos): `go.mod` (`github.com/cslsoftwares/mcpvmware`, depende de `github.com/vmware/govmomi v0.55.1`), `cmd/mcpvmware-mcp/main.go` (entrypoint stdio JSON-RPC, flags `--vcenter-url/--username/--password/--insecure`), `mcp/types.go` (tipos MCP/JSON-RPC), `tools/registry.go`+`tools/system.go` (2 tools seed: `vmware_about`, `vmware_list_vms`), `vmware/client.go` (wrapper sobre `govmomi.NewClient`+`find.Finder`). Arquitetura de referência: `D:\MCPNas\truenas-mcp`. Verificado no disco em 09/08/2026 21:50 (não só por `.workspace/context.json` — confirmado por `ls` real).
- **2026-08-09 21:47-21:52** — Teste empírico contra `10.100.2.54` (via curl, sem credenciais): TLS self-signed confirmado (`curl` sem `-k` falha com erro 60); `/ui/` responde 200 (vSphere Client); **`POST /sdk` com `RetrieveServiceContent` real (não-autenticado por natureza do protocolo SOAP) confirma definitivamente: é um ESXi standalone 7.0.3 build 21930508 patch 0.95 (`apiType: HostAgent`, MOIDs `ha-*`) — NÃO é vCenter.** REST/VAPI (`/api/session` e `/rest/com/vmware/cis/session`) devolve 400 Bad Request corpo vazio mesmo com Basic auth dummy e headers corretos — inconclusivo sem credencial real, possivelmente API REST não totalmente exposta neste host ESXi bare.
- **2026-08-09 21:57** — **Teste autenticado completo e bem-sucedido** contra `10.100.2.54` (usuário `root`, senha fornecida pelo usuário via chat — não persistida em nenhum arquivo): build limpo (`go build ./cmd/mcpvmware-mcp`) + execução real via stdio JSON-RPC exercitando o pipeline inteiro:
  - `initialize` → OK (protocolVersion 2024-11-05)
  - `tools/list` → OK (2 tools: `vmware_about`, `vmware_list_vms`)
  - `tools/call vmware_about` → OK, dados reais batendo com a sonda SOAP anterior (ESXi 7.0.3 build 21930508, `api_type: HostAgent`)
  - `tools/call vmware_list_vms` → OK, **1 VM real encontrada**: `/ha-datacenter/vm/vmfs/volumes/6a72b614-3e3ede6b-a2f7-d4ae52918af0/cac-WN02/cac-WN02.vmx` (nome `cac-WN02`)
  - Login/sessão via `govmomi.NewClient` com `Insecure: true` funcionou de primeira — confirma que o wrapper `vmware/client.go` está correto contra um host real.
- **2026-08-09 22:20-22:33** — Pesquisado `github.com/vmware` + `github.com/vmware-archive` por repositórios úteis ao projeto. **Achado que corrige a linha de VAMI abaixo:** `src/vapi/` (na época ainda o vendor do govmomi) tem subpacotes `appliance/{access,logging,networking,shutdown}`, `authentication`, `cis`, `cluster`, `crypto`, `esx`, `library` (Content Library), `namespace`, `rest`, `tags`, `vcenter`, `vm` — ou seja, **VAMI é parcialmente coberto pelo govmomi**, não "sem equivalente" como registado antes (corrigido na tabela "Active architecture"). Confirmado também que `vmware/vsphere-automation-sdk-go` (SDK Go oficial p/ REST/VAPI) segue em Beta e **ainda não suporta vSphere/vCenter** (só VMC/NSX-T) — não é opção viável; `govmomi`'s próprio `vapi/rest` é o único cliente REST/VAPI Go-nativo disponível. Repos de referência de padrão (não dependências): `terraform-provider-vsphere` (671★, Go maduro sobre govmomi em produção) e `packer-plugin-vsphere` (126★, idem p/ criação de templates). Em `vmware-archive`: `vsphere-automation-sdk-rest` (196★, arquivado — confirma que as coleções Postman já vendorizadas em `.workspace/` são a referência REST mais atual disponível) e `cloud-init-vmware-guestinfo` (196★, arquivado — mecanismo já trazido indiretamente via dependência `vmw-guestinfo` do próprio govmomi).
- **2026-08-09** — Usuário clonou 8 repos de referência em `outros/` (govmomi + 3 SDKs oficiais + 3 MCP servers da comunidade + ssh-mcp-server genérico) e pediu análise. 4 agentes Explore em paralelo (sonnet p/ os 3 MCP servers, haiku p/ checagem rápida dos SDKs+ssh): **nenhum dos 3 MCP servers da comunidade resolve VAMI** — `vmware-vcenter-mcp` é 100% SOAP/pyVmomi sem nenhuma tool de appliance (dependência do SDK REST oficial no requirements.txt está morta, nunca importada); `vmware-esxi-mcp` é essencialmente um **mock** (nunca chama pyVmomi de verdade, testes referenciam módulos inexistentes, CI mascara falhas); `vmware-vsphere-mcp-server` é o mais real (híbrido REST+pyVmomi, 41 tools) mas também sem VAMI. Confirmam que os padrões já adotados no scaffold Go estão certos (`confirm:bool` p/ destrutivas, JSON estruturado em vez de Markdown solto, uso de PropertyCollector em lote em vez de N+1 REST calls) e expõem 2 bugs reais a NÃO replicar (`hostname` decorativo/ignorado, TLS `CERT_NONE` hardcoded ignorando a flag insecure). SDKs oficiais Java/Python: aviso de migração p/ "VCF SDK" em releases 9.0.0.0+. Conclusão: Fase 4 (VAMI) do plano continua sem precedente demonstrado — terreno próprio, como já assumido.
- **2026-08-09 22:48-22:55** — **Usuário reorganizou as pastas do repositório** (fora do meu controle, feito diretamente por ele): `src/` **inverteu de significado** — deixou de ser o vendor do govmomi e passou a ser o **módulo Go PRÓPRIO do MCPVMWare** (movido pra lá: `go.mod`, `mcpvmware-mcp/` [renomeado de `cmd/mcpvmware-mcp/`], `mcp/`, `tools/`, `vmware/`). `outros/` foi renomeado para `referencia/` e ganhou um clone próprio do govmomi (`referencia/govmomi/`) — todo o material de terceiros agora vive só em `referencia/`, fora de `src/`. Reconfirmado por mim (não só aceito por relato): `cd src && go build ./... && go vet ./...` limpo, `gofmt` sem apontamentos; `src/cmd/` (pasta vazia remanescente do move) removida; `referencia/govmomi` confirmado como o mesmo clone limpo (mesmo remote, working tree limpa). Atualizados `README.md`, `.workspace/context.json`, a rule local (`_V1.2.0`→`_V1.3.0`) e o próprio plano aprovado (`streamed-imagining-lightning.md`, todos os paths `src/...` que referenciavam fonte do govmomi corrigidos para `referencia/govmomi/...`) com os caminhos novos.
- **2026-08-10 03:04-03:30** — **Reanálise completa de `referencia/` + `src/` + material solto em `.workspace/`**, a pedido do usuário ("veja com detalhe tudo que é importante"). Achados:
  - Plano aprovado (`.workspace/plans/MCPVMWare2026-08-09-224451-plano-implementacao-tools-mcp.plan.md`) tinha paths stale de antes da reorg de pastas (`src/session/keepalive/...`, `cmd/mcpvmware-mcp` etc. quando devia ser `referencia/govmomi/...` / `src/mcpvmware-mcp`) — corrigido; nova secção "Decisões em aberto" (guest ops/VIX, domínios dos 3 MCP concorrentes, VixDiskLib) documentada, nenhuma bloqueia a Fase 2.
  - **Achado de processo importante:** este ambiente não tem `pdftoppm`/poppler instalado — a tool `Read` falha em qualquer PDF. 2 subagents Haiku, ao tentar "ler o PDF", bateram nesse erro e **inventaram sumários falsos** (page counts, estrutura) em vez de reportar a falha — só confirmado como fabricação ao verificar eu mesmo com `pdftotext` (que funciona) e comparar. Ver `.wolf/cerebrum.md` Do-Not-Repeat — nunca mandar um subagent "ler PDF" nesta máquina sem pré-extrair com `pdftotext` primeiro.
  - Conteúdo real (via `pdftotext`, não a tool Read) dos 5 PDFs em `referencia/`: `vmware-vsphere-sdks-and-tools-{6-5,6-7,7-0}.pdf` são os guias oficiais de SDK — confirmam por fonte primária que só existem bindings oficiais Java/C#/.NET/Python/Perl (**nunca Go**) tanto para o "Web Services SDK" (SOAP, o que `govmomi` espelha) quanto para o "vSphere Automation SDK" (REST/VAPI, maturidade crescente 6.5→7.0 — em 7.0 já é um "Programming Guide" completo, capítulo próprio); cobrem também Storage Policy/SPBM, CIM SMASH, VDDK, Guest SDK, ESXCLI, OVF Tool, vSphere Client SDK (plugins UI). `vmware-vsphere-sdks-and-tools-9-1.pdf` **não tem** capítulo de Web Services SDK nem Automation SDK — só VDDK, OVF Tool e vSphere Client SDK (plugins) — sinal de que a doc de API SOAP/REST "clássica" pode ter migrado para outra publicação nas versões mais recentes (não investigado a fundo). `vmware-vsphere-7-0.pdf` é a **documentação geral do produto** (release notes, instalação/upgrade ESXi/vCenter) — não é um doc de API, baixa relevância para o MCP.
  - `.workspace/DADOS_CONEXAO_TESTE.md` e `.workspace/config_teste.json` são resíduo de outro projeto (ProvidersORM/GestorERP — credenciais de bancos/LDAP de teste em `10.100.2.3`) — **não relacionados a este projeto**, já no `.gitignore` (confirmado com o owner 09/08/2026), sem ação necessária. `.workspace/Guia_Instalacao_Remota_Windows_11.md` idem, clutter não-VMware.
  - `anatomy.md` estava desatualizado (não indexava `src/`, `referencia/`, `.workspace/`) — rodado `openwolf scan` (508 arquivos reindexados).
- **2026-08-10 16:39** — **Nova tool `vmware_datastore_upload_file`** (`tools/datastore.go`), fora do escopo original das 6 fases — usuário perguntou "consigo fazer upload de ISO?" via chat, esclarecido (AskUserQuestion) que `local_path` é lido do disco da máquina que roda o `mcpvmware-mcp.exe` (não um upload direto do cliente MCP — o protocolo não tem esse mecanismo), usuário confirmou que serve e pediu para implementar. `object.Datastore.UploadFile` (govmomi) + guarda de sobrescrita (`Datastore.Stat` antes — recusa se já existe, a menos que `overwrite:true`). Sem tier/gate — não é Tier1/2 por definição do projeto (cria/substitui um arquivo, não destrói VM/host), mas a guarda de sobrescrita evita perda de dado acidental. `tools/datastore_test.go` — 2 testes reais contra o serviço HTTP de datastore do **vcsim de verdade** (`simulator/host_datastore_browser.go` + `Service.ServeDatastore`, não fixture): upload→upload sem overwrite nega (arquivo já existe)→upload com overwrite:true passa; validação dos 3 args obrigatórios. `go build/vet/gofmt/test` limpos (29 testes nos 2 pacotes), binário recompilado. **Total: 29 tools registadas.**
- **2026-08-10 17:04-17:05** — **`vmware_datastore_upload_file` verificada ponta-a-ponta contra o host real** — usuário pediu upload de 4 ISOs reais de instalação ESXi 7.0.3 (Downloads, ~1.6GB total) para o datastore `Dado-HD` (escolhido pelo usuário via AskUserQuestion), pasta `ISO/`. Achado no caminho: a tool original recusava upload se a pasta destino não existisse (`remote_path` sem criar pastas-pai) — **corrigido antes de rodar**: `object.FileManager.MakeDirectory(ctx, ds.Path(dir), nil, true)` best-effort (erro engolido de propósito — "já existe" é o caso comum, qualquer problema real ainda aparece no `UploadFile` seguinte) adicionado a `handleDatastoreUploadFile`. Os 4 uploads reais, via binário real dirigido por stdio (subprocess, background): **100% sucesso** — 19.3s / 21.7s / 30.4s / 16.7s (~88s total, ~18MB/s médio). `remote_path` final confirmado: `ISO/VMware-ESXi-7.0.3-20036589-HPE-703.0.0.10.9.1.5-Jul2022.iso` e as 3 variantes `VMware-VMvisor-Installer-7.0U3{d,f,n}-*.iso`, todas em `/ha-datacenter/datastore/Dado-HD`.
- **2026-08-10 16:33** — **Smoke real contra `10.100.2.54` bem-sucedido — desbloqueia o item pendente de TODAS as fases de leitura.** Host estava desligado (confirmado pelo usuário), religado, rede confirmada de volta (ping+curl) após ~10min; binário `mcpvmware-mcp.exe` real dirigido via stdio JSON-RPC (subprocess Go throwaway, scratchpad) contra o host real, não vcsim:
  - `vmware_about`: ESXi 7.0.3 build 21930508 (confere com sessões anteriores).
  - `vmware_list_vms`: 1 VM — `/ha-datacenter/vm/cac-WN02` (confere).
  - `vmware_list_hosts`: `/ha-datacenter/host/localhost.cslsolucoes.com.br/...` — hostname real do ESXi.
  - `vmware_list_datastores`: **4 datastores reais** — `Dados-RT` (8GB), `TrueNAS-iSCSI-esxi70u3` (2.1TB), `Dados-NV` (2.6TB), `Dado-HD` (1.8TB), todos `VMFS`/`accessible:true`, com capacidade/espaço livre reais.
  - `vmware_list_networks`: 3 — `VM Network 0/1/2 - 10G`.
  - `vmware_list_resource_pools`: 1 (implícito "Resources").
  - `vmware_list_clusters`: **0, sem erro** — confirma a correção do bug-002 (`dcScopedPath`/`emptyOnNotFound`) funcionando contra o host real, não só vcsim.
  - `vmware_list_datacenters`: 1 — `/ha-datacenter`.
  - `vmware_host_info`: hardware real — Dell PowerEdge R720, Intel Xeon E5-2697 v2, 24 cores, ~274GB RAM, `connected`/`poweredOn`/não em maintenance mode.
  - `vmware_host_management_ips`: `10.100.2.54` (correto).
  - **Todas as tools de leitura das Fases 0/2/3 confirmadas contra o host real.** Únicos itens ainda pendentes: as tools destrutivas (Tier 1/2 — `vm_destroy`, `power_off/reset/suspend`, `snapshot_revert/remove`, `host_maintenance_enter`) e VAMI (Fase 4, estruturalmente não-testável neste host standalone) — aguardam combinação explícita adicional do usuário antes de qualquer execução real, por politica já registada no plano.
- **2026-08-10 16:15-16:25** — **Fase 4 implementada e verificada** (`tools/appliance.go` — 4 tools starter de VAMI: `vmware_appliance_version`, `vmware_appliance_uptime`, `vmware_appliance_health`, `vmware_appliance_health_detail`), última fase do plano ("prossiga sem pausas todas as fases"):
  - Endpoints VAMI (`GET rest/appliance/system/version`, `.../system/uptime`, `.../health/{system,applmgmt,database-storage,load,mem,software-packages,storage,swap}`) confirmados por leitura estruturada (Python) da própria coleção Postman já vendorizada (`.workspace/vSphere Automation REST Resources for appliance.postman_collection.json`) — não adivinhados.
  - Decisão deliberada: **sem struct tipado** para as respostas VAMI (decode genérico `interface{}`) — não há simulador nem vCenter Appliance real disponível pra validar nomes de campo contra a coisa real; um struct adivinhado poderia esconder/renomear campos silenciosamente. `health_detail` agrega os 8 subsistemas numa chamada por subsistema, tolerando falha individual (`{"error":...}` inline) em vez de falhar tudo.
  - **Achado de teste real:** nem SOAP (vcsim) nem `/appliance/...` (VAMI/REST) precisam estar no MESMO servidor para testar — `vmware.Client.REST(ctx)` só depende do `*vim25.Client` embutido (nunca toca `ServiceContent`), então dá pra construir um `*vmware.Client` mínimo (`&vmware.Client{Client: &govmomi.Client{Client: &vim25.Client{Client: soap.NewClient(fixtureURL, true)}}}`) apontado direto a um fixture `httptest.Server`, sem vcsim nenhum — exactamente o "teste unitário (fixture HTTP)" que o plano previa, confirmado funcionando na prática.
  - `tools/appliance_test.go` — 5 testes: version/uptime/health individuais contra o fixture; `health_detail` agrega os 8 subsistemas; 1 subsistema falhando (404 simulado) não derruba a chamada agregada inteira.
  - `go build/vet/gofmt/test` limpos (27 testes nos 2 pacotes). **Smoke end-to-end extra** (não só `go test`): binário `mcpvmware-mcp.exe` real, compilado, dirigido via stdio JSON-RPC (`initialize`→`tools/list`→4×`tools/call`) contra um vcsim `VPX()` real através de um subprocess Go throwaway (scratchpad) — confirma **28 tools registradas** e respostas correctas ponta-a-ponta pelo binário de facto, não só pelos testes de pacote. Limitação de não-verificável-ponta-a-ponta contra VAMI real permanece documentada (nem o host real disponível — ESXi standalone — nem o vcsim simulam `/appliance/system/version` ou `/appliance/health/*`).
  - **Todo o plano aprovado (Fases 0, 1a, 1, 2, 3, 4) está agora implementado e verde no vcsim/fixture.** Único item pendente em todas as fases: os smokes manuais contra `10.100.2.54` real — bloqueados por falta de rota de rede nesta sessão, não por nenhum problema de código.
- **2026-08-10 16:07-16:15** — **Fase 3 implementada e verificada** (`tools/host.go` — 5 tools: `vmware_host_maintenance_enter` [Tier2], `vmware_host_maintenance_exit`, `vmware_host_reconnect`, `vmware_host_management_ips`, `vmware_host_info`), a pedido do usuário ("prossiga sem pausas todas as fases"): `resolveHost` usa `dcScopedPath("host", ...)` (mesmo padrão da Fase 1/2). `tools/host_test.go` — 5 testes: ciclo maintenance enter→info(true)→exit→info(false); gate fechado nega enter sem tocar o host; enter sem confirm nega; management_ips + reconnect não erroram; info tem todos os campos esperados e reporta `poweredOn` pro host real. `go build/vet/gofmt/test` limpos (22 testes no pacote `tools`), binário recompilado. Smoke manual contra `10.100.2.54` continua bloqueado por rede.
- **2026-08-10 15:53-16:05** — **Fase 1 implementada e verificada** (`tools/vm.go` — 11 tools: `vmware_vm_power_on/off/reset/suspend`, `vmware_vm_reconfigure`, `vmware_vm_destroy`, `vmware_vm_snapshot_create/revert/remove/list`, `vmware_vm_info`), a pedido do usuário ("continue" após Fase 1a):
  - Tier1 (`destroy`, `snapshot_revert`, `snapshot_remove`) e Tier2 (`power_off`, `reset`, `suspend`) registados via `r.registerDestructive(...)` da Fase 1a — primeira vez que tools reais (não dummy) passam pelas 3 camadas.
  - `resolveVM` usa `dcScopedPath("vm", ...)` (mesmo fix da Fase 2) — funciona em vCenter multi-DC e standalone.
  - **1 achado real via teste, não assumido:** `Destroy_Task` do vSphere rejeita VM ligada (`InvalidPowerState`) — `vmware_vm_destroy` **não** desliga a VM por conta própria (decisão deliberada: uma tool Tier1 fazer trabalho destrutivo extra silenciosamente seria pior, não mais seguro) — descrição da tool avisa disso explicitamente. `TestVMTools_Destroy` desliga a VM antes de destruir.
  - Confirmado (não assumido) que `VirtualMachineConfigSpec.NumCPUs`/`MemoryMB` com `omitempty` são seguros de deixar no zero-value do Go quando só um dos dois é passado no `reconfigure` — não zera o outro campo (`TestVMTools_Reconfigure`).
  - `tools/vm_test.go` — 6 testes: gate fechado nega `destroy` sem tocar a VM; ciclo completo power on→reset→suspend→power on→power off com `vmware_vm_info` a confirmar cada estado; `power_off` sem `confirm` falha E não muda o estado; `reconfigure` muda `num_cpus` sem zerar `memory_mb`, e falha sem nenhum dos dois; ciclo completo de snapshot create→list→revert(nega sem confirm)→revert→remove→list(0); destroy completo (desliga→destrói→`vm_info` já não resolve).
  - `go build/vet/gofmt/test` limpos (17/17 testes no pacote `tools`), binário recompilado. Smoke manual contra `10.100.2.54` continua bloqueado por falta de rota de rede — ver "External blockers".
- **2026-08-10 15:46-15:50** — **Fase 1a implementada e verificada** (proteção em 3 camadas para ações destrutivas, decidida via AskUserQuestion): `src/tools/destructive.go` novo — `tier` (1/2), `Registry.registerDestructive`/`wrapDestructive` (gate → confirm estrito → handler → auditoria, nessa ordem; negação em qualquer camada ainda grava auditoria), `writeAudit` (JSON Lines, `os.OpenFile` append por chamada, sem handle persistente). `Registry`/`NewRegistry` ganharam `RegistryOptions{AllowDestructive, AuditLogPath}` — mudança de assinatura, todos os call-sites atualizados (`main.go`, `inventory_test.go`). `mcpvmware-mcp/main.go` ganhou `--allow-destructive`/`VCENTER_ALLOW_DESTRUCTIVE` e `--audit-log-path`/`MCPVMWARE_AUDIT_LOG` (mesmo padrão dos flags existentes), com aviso no arranque quando destrutivo está ligado. `tools/destructive_test.go` — 4 testes via vcsim usando uma tool Tier1 dummy registada só no teste (nenhuma tool real Tier1/2 existe ainda — só a Fase 1/3 as escreve): gate fechado nega ANTES do handler correr (prova via flag `called`); `confirm` estrito (rejeita string/int truthy, só bool exacto); chamada permitida corre o handler E grava auditoria; chamada negada também grava auditoria. `go build/vet/gofmt/test` limpos, binário recompilado.
- **2026-08-10 15:29-15:50** — **Fase 2 implementada e verificada** (`tools/inventory.go` — 6 tools de listagem: `vmware_list_hosts`, `vmware_list_datastores`, `vmware_list_networks`, `vmware_list_resource_pools`, `vmware_list_clusters`, `vmware_list_datacenters`), a pedido do usuário ("execute o plano... tem que funcionar para VCenter e VMware VSphere Standalone"):
  - **2 bugs reais achados e corrigidos por evidência (vcsim), não por teoria** — ambos em `.wolf/buglog.json`:
    - **bug-001:** `tools.newSimClient` (harness da Fase 0, nunca antes exercitado por um teste real) tinha duplo-login — `s.URL` do vcsim vem com `user:pass@` embutido, o que dispara auto-login do `govmomi.NewClient` antes do nosso `Login` explícito, que então falha. Fix: `u.User = nil` antes de montar `vmware.Config`.
    - **bug-002 (o que justifica "tem que funcionar para VCenter e Standalone"):** `client.Finder` sem datacenter default (vCenter com 2+ datacenters — caso em que `vmware.NewClient` não consegue resolver `DefaultDatacenter` sem ambiguidade) quebra QUALQUER list method com path relativo tipo `"*"` — erro "please specify a datacenter". Isto já era um bug latente no `vmware_list_vms` da Fase 0, nunca detectado porque o único alvo real (`10.100.2.54`) é ESXi standalone (sempre resolve sem ambiguidade). Fix: `tools/finder.go` novo — `dcScopedPath(folder, path)` (constrói path absoluto com datacenter em wildcard, ex. `/*/host/*`) + `emptyOnNotFound(err)` (converte `find.NotFoundError` — devolvido em vez de lista vazia — em `nil`, para tolerar 0 clusters num ESXi standalone sem virar erro). Aplicado também a `vmware_list_vms` (retrofit).
  - **Evidência lida, não assumida:** `inventory_test.go` prova as duas topologias via `vcsim` — `TestInventoryTools_MultiDatacenterVCenter` (`simulator.VPX()` com `Model.Datacenter=2`, as 6 tools com count>0 across 2 DCs) e `TestInventoryTools_StandaloneESXi` (`simulator.ESX()`, 1 host/1 DC/0 clusters tolerado/pool implícito "Resources"). `go build ./... && go vet ./... && gofmt -l . && go test ./... -v` — todos limpos, 100% PASS (`vmware`: 2/2 cacheados; `tools`: 11/11 novos + já existentes).
  - **Pendente/bloqueado:** critério de aceite #5 (lista manual contra `10.100.2.54` real) **não executado** — esta sessão/ambiente não tem rota de rede para `10.100.2.0/24` (`ping`/`curl` a `10.100.2.54` dão timeout consistente). Binário `src/mcpvmware-mcp.exe` já recompilado com as tools novas; `.mcp.json` já configurado (`mcpVmwareTest`, root/senha fornecida pelo usuário via chat) para quando a rede/sessão permitir.
  - `.mcp.json` (config de MCP servers do Claude Code, não do projeto) ganhou a entrada `mcpVmwareTest` apontando pro binário local com as credenciais reais de `10.100.2.54` — ficheiro **não estava** no `.gitignore` apesar do seu próprio `_README` alegar isso (corrigido: `.mcp.json` adicionado ao `.gitignore`, confirmado não-tracked antes da correção).
- **2026-08-09 23:00-23:08** — **Phase 0 implementada e verificada** (sessão/keepalive + harness vcsim, conforme plano):
  - `src/vmware/client.go` reescrito: `NewClient` conecta SEM userinfo → `gc.RoundTripper = keepalive.NewHandlerSOAP(gc.RoundTripper, idle, nil)` → **depois** `gc.Login(...)` (ordem confirmada lendo `referencia/govmomi/session/keepalive/handler.go` — o ticker só arma ao observar o round-trip de login). Extraído `newClient(ctx, cfg, idle time.Duration)` interno (idle configurável) — `NewClient` público chama com a constante de produção (`keepAliveIdle = 10*time.Minute`), testes chamam com idle comprimido. Adicionado accessor lazy `REST(ctx)` (Phase 4, ainda não usado por nenhum tool) e `Close` agora desloga a sessão REST também, se alguma vez foi aberta.
  - `src/tools/testhelpers_test.go` — `newSimClient` (helper compartilhado pros próximos arquivos de teste por domínio) sobe `simulator.Model`/`NewServer` e conecta via `vmware.NewClient` de verdade (não bypassa login/keepalive).
  - `src/vmware/client_keepalive_test.go` — 2 testes reais contra `simulator.Test`: `TestNewClientKeepAliveSurvivesSessionTimeout` (usa `sim25.SetSessionTimeout` pra comprimir o timeout do servidor a 250ms e um keepalive idle de 125ms — **prova de verdade** que a sessão sobrevive, não só "compila") e `TestNewClientLogsOutAndStopsKeepAlive` (prova que `Close` desloga mesmo).
  - **Evidência lida, não assumida:** `cd src && go build ./... && go vet ./... && gofmt -l .` — todos limpos (0 saída/erros). `go test ./... -v`: **2/2 PASS** em `vmware` (`TestNewClientKeepAliveSurvivesSessionTimeout` 0.86s, `TestNewClientLogsOutAndStopsKeepAlive` 0.36s); `tools` sem testes ainda (só o helper, esperado — os testes por domínio vêm nas próximas fases); `mcp`/`mcpvmware-mcp` sem testes (esperado, são tipos/entrypoint).
  - **Pendente:** critério de aceite #3 do plano (rebuild + smoke manual `vmware_about`/`vmware_list_vms` via stdio contra `10.100.2.54` real) — não executado ainda, precisa da credencial `root` de novo (não persistida em nenhum arquivo). A evidência automatizada (testes acima) já prova a lógica de keepalive; o smoke real só confirmaria que a MESMA sequência de login funciona contra hardware de verdade (já era o caso antes desta fase, mudou só a ordem interna de wiring do round-tripper).

---

## 🚀 Next phase

**2026-08-20 — AÇÃO PENDENTE DO USUÁRIO: reconectar os 4 servidores MCP no painel.**
O binário `1030-allurl` está em `D:\ServidorDataCenter` e o `.mcp.json` já aponta VM01/02/03 para
`--all-url` (mostrarão **1030** tools; VM Test segue `--vmware-all-url` = 907). Basta dar Reconnect nos 4.

**Código do MCP essencialmente completo: 1030 tools (FT já implementado nas ondas — `generated_vm_ft.go`).**
Toda a camada de API do lab (host/cluster/rede/storage/vMotion/HA/DRS/**FT**) está coberta.
- **→ Opcional (não pedido):** nichos residuais do vim25 (SmartCard, DirectPath, HostPatch, Kerberos,
  HostAccessManager, ~40 tools) — o proxy ainda os lista, mas a maioria é plumbing interno
  (PropertyCollector/SessionManager/ViewManager) que não faz sentido como tool MCP.
- **→ Pendências de AMBIENTE (lab e-book, não de código):** confirmar switches **stack/MLAG × independentes**;
  renomear VM03 (`localhost`→`vm-csl03`) e unificar DNS/PTR (`csltech.local` × `cslsolucoes.com.br`); habilitar
  **EVC=Westmere** ao criar o cluster; apontar os MCPs para o **VCSA 7.0u3** greenfield quando existir.
- **→ Limpeza sugerida:** `git rm --cached src/gen.exe src/mcpvmware-mcp.exe` (binários stale tracked, ~27MB) + gitignore.

---

**2026-08-19/20 (sessão longa) — EXPANSÃO ALÉM DAS 734: preenchidas as frestas do
gerador (métodos vim25 SEM wrapper `object.*`). Atual: ~1030 tools, TODAS
commitadas+pushadas em `origin/main`. Git por onda (a pedido do usuário).**

Commits desta sessão (9 ondas):
- `262c389` (734→811): iSCSI completo (22), FT (7), Guest Ops (15), Perf (9),
  License (13), Alarm (10), query_port_groups (1).
- `ecd3506` (811→838): Crypto/KMS SOAP CryptoManagerKmip (27).
- `c89a6b3` (838→866): DVS + DVSManager (28).
- `2c79108` (866→892): HostSystem restantes (13) + ClusterComputeResource (13).
- `90c8ca3` (892→931): HostProfileManager (21) + HealthUpdate (10) + IpPool (8).
- `45bcb6b` (931→971): First-Class Disks / vStorageObject vCenter (22) + host (18).
- `dc468d0` (971→1001): vSAN (19) + Guest Registry/Alias (11).
- onda 9 (1001→~1030): vFlash (5) + IoFilter (8) + Datacenter/Event (9) +
  ScheduledTask (7). [commit em curso ao escrever isto]

**MAPA DE FRESTAS ESSENCIALMENTE ESGOTADO.** Sobram só nichos residuais (SmartCard,
DirectPath, AnswerFile, HostPatch parcial, HostAccessManager, Kerberos, métodos
avulsos) + o plumbing interno deliberadamente excluído (PropertyCollector/
SessionManager/ListView/MonitoredEntities). Mapa por MO: scratchpad
`soap_all.txt`/`mo_method.txt`.

**DEPLOY DAS 1030 CONCLUÍDO — 20/08 12:44.** Produção
(`D:/ServidorDataCenter/mcpvmware-mcp.exe`) = build de 1030 tools (hash SHA256
`344DC318...`, 27.5 MB). Smoke real do binário de produção contra o ESXi
10.100.2.58 confirmou **907 tools no modo `all`** (fcd 22, fcdhost 18, vsan 23,
dvs 33, crypto 30, host_profile 21, iofilter 8, scheduledtask 7, guest_registry
6 — todas presentes). Tudo em `origin/main` (`bf73e43`). **PENDÊNCIA DO USUÁRIO:
reconectar os 4 servidores MCP no painel** (os processos foram mortos para o
deploy; ao reconectar, VM01/02/03 sobem com 907 e o Test com 907 no modo all).

**Lições (todas no cerebrum):** (1) frestas = vim25 sem wrapper `object.*` → raw
`methods.*`; (2) bug deadlock vcsim guest ops; (3) 1 vcsim por arquivo de teste;
(4) gate = cada pacote em processo separado + RETRY (o `tools` flaka por contenção
de ~450 vcsim, piora a cada onda; `-p 1` não basta); (5) subagentes às vezes
sub-delegam/colidem — anti-delegação + verificar disco, nunca aceitar "verde"
vago; (6) NÃO juntar `go test | tail; git commit` no mesmo comando (o exit vira
do echo — commitei com FAIL uma vez).

---

**O plano de cobertura completa (Fases 0-10) está 100% CONCLUÍDO —
12/08/2026 ~19:15.** `.workspace/plans/MCPVMWare2026-08-10-175300-plano-cobertura-completa-api-codegen.plan.md`
fechado (frontmatter `status: concluído`). **734 tools registadas no
total**, cobrindo 3 produtos VMware distintos via 3 clientes/arquitecturas:
- **vSphere/vCenter/ESXi** (`govmomi`, `src/vmware/`) — 611 tools, via 2
  técnicas de geração de código (AST sobre `object/`+`vapi/*/*.go`, Fases
  1-8a) + parse de collection Postman (VAMI legacy, Fase 8b).
- **VMware Workstation Pro** (`vmrest`, `src/workstation/`) — 28 tools,
  Fase 9, hand-written, cliente HTTP+Basic Auth próprio.
- **VMware Cloud on AWS** (`src/cloudaws/`) — 95 tools, Fase 10,
  hand-written, cliente HTTP com auth CSP token-exchange próprio.

**O que NÃO está implementado, deliberadamente, e fica pra uma decisão
futura do usuário (não bloqueia o "100%" já atingido — são extensões, não
gaps de cobertura):**
1. `--vmware-all-url` incluir tools de Workstation/CloudAWS — exigiria
   `main.go` aceitar múltiplas URLs/credenciais em simultâneo (1 backend
   por conexão hoje). `Registry` já tem capacidade estrutural pra guardar
   os 3 clientes (`registry.go`), só falta a UX de flags pra alimentar
   mais que 1 de uma vez. Sinalizado desde 10/08/2026, nunca resolvido
   silenciosamente.
2. Os achados de prioridade MÉDIA/BAIXA da revisão da Fase 0 original
   (itens 5-9, ver plano — ex.: alguns zero-arg accessors ainda por
   auditar em domínios tardios) — nunca bloquearam nenhuma fase, ficam
   como item de limpeza opcional.
3. Nenhum dos domínios hand-written (VAMI legacy, Workstation, VMC on AWS)
   tem verificação ponta-a-ponta contra um serviço real — só fixture
   `httptest`, por falta de ambiente de teste disponível neste projecto
   (nem VAMI real, nem `vmrest` local nesta sessão, nem conta VMC).
4. Nenhum commit foi feito em nenhuma fase desta sessão — "ok, finalizar
   100%" autorizou avançar pelas fases, não fazer commit; se o usuário
   quiser, revisar `git status`/`git diff` primeiro (volume grande de
   ficheiros novos).

**Padrão de execução consolidado ao longo de toda a sessão** (reutilizável
em qualquer fase futura do projecto): (1) revisão humana do sub-conjunto
de métodos/rotas ANTES de gerar, nunca confiar cegamente na classificação
automática; (2) domínios grandes (>~20 tools) → subagents paralelos por
ficheiro, domínios pequenos → escrito directamente; (3) instruções
explícitas de "nunca terminar vago"/verificação em 2 corridas de
`go test`; (4) confirmar suporte real de qualquer simulador/fixture por
evidência (grep/spike), nunca assumir — achou um simulador REST/VAPI
inteiro nunca antes usado no projecto (Fase 8a) por causa disto; (5) para
domínios sem wrapper Go, parse directo da fonte canónica (collection
Postman) em vez de adivinhar rotas (Fases 8b/9/10); (6) quando um domínio
precisa de um cliente/arquitectura nova, construir e verificar essa infra
directamente antes de delegar (nunca delegar plumbing de baixo nível que
afecta tools já existentes) — Fases 9 e 10 seguiram isto à risca, ambas
sem regressão nenhuma nas tools pré-existentes.

**Correção 10/08/2026 ~19:20:** `--vmware-all-url` não é "1 endpoint vSphere
sem filtro" — é **múltiplos backends simultâneos** (ESXi ou vCenter **e**
Workstation Pro ao mesmo tempo), correção do próprio usuário à ambiguidade
que eu tinha sinalizado. Implica que `Registry` precisa de 2 clientes vivos
(não só filtro de `tools/list`) — detalhe de arquitectura novo na Fase 0.

**Decidido 10/08/2026 ~19:15: VMware Cloud on AWS entra no "100%"**, flag
própria `--cloud-aws-url`, nova **Fase 10** no plano (auth CSP token,
99 tools, cliente HTTP totalmente à parte de `govmomi`/`vmrest` — custo
financeiro real de operações destrutivas em SDDC, tier mais cauteloso que
o padrão). Alvo total do plano subiu para **~1.002 tools**.

**Achado 10/08/2026 ~19:00 (usuário perguntou "está atendendo 100%?"
cross-referenciando as 8 coleções Postman de `.workspace/`):** 2 gaps reais
achados por contagem exacta, não estimativa — (1) **VAMI/appliance**: a
coleção tem 132 rotas, `vapi/appliance` do govmomi só tem 15 métodos
tipados (89% sem wrapper Go) — Fase 8 do plano precisou de uma 2ª técnica
de geração (parse directo da coleção Postman + chamadas REST genéricas,
não AST sobre Go) pra fechar esse gap; alvo total do plano subiu de ~786
pra **~903 tools**. (2) **VMware Cloud on AWS** (99 rotas catalogadas,
produto distinto, sem relação com `govmomi`) continua **fora** do plano —
sinalizado como decisão aberta, não incluído sem confirmação explícita.
Content Library (69 métodos Go vs 21 rotas) confirmado bem coberto, sem gap.

**Adição 10/08/2026 ~18:55 (a pedido do usuário):** o plano ganhou uma secção
"Modos de conexão / seleção de produto" — 4 flags mutuamente exclusivas
(`--vcenter-url`=vCenter-only+geral, `--vmware-url`=ESXi standalone só geral,
`--workstation-url`=só as 28 do Workstation Pro, `--vmware-all-url`=tudo
vSphere incondicional, comportamento actual preservado) que filtram
`tools/list` por produto. **Risco de compatibilidade sinalizado, não
resolvido:** os 2 `.mcp.json` já em produção usam `--vcenter-url` apontando
pra `10.100.2.54`, que é ESXi standalone, não vCenter — nome errado sob a
semântica nova. Usuário precisa decidir entre renomear os `.mcp.json` pra
`--vmware-url` ou manter `--vcenter-url` com comportamento permissivo por
compatibilidade. Também sinalizada ambiguidade não resolvida em
`--vmware-all-url` (interpretado como "1 endpoint, todas as tools vSphere"
— não "múltiplos backends simultâneos").

**Novo plano aprovado pelo usuário, ainda não iniciado (10/08/2026 ~17:53):**
cobertura "100% das APIs" — literalmente cada método SOAP + rota REST —
via **geração de código** (não hand-authoring como as 29 tools actuais).
Números reais medidos: `object/` (448 métodos, 56 ficheiros) + `vapi/*`
(310 métodos, 9 subpacotes) = **~758 tools alvo (govmomi, Fases 0-8)** +
**28 tools do `vmrest`** (VMware Workstation Pro — produto distinto, sem
`govmomi`, Fase 9, adicionada 10/08/2026 ~18:45 a pedido do usuário
apontando a collection já catalogada). Total ~786. Plano completo:
`.workspace/plans/MCPVMWare2026-08-10-175300-plano-cobertura-completa-api-codegen.plan.md`.
**Próximo passo real: Fase 0 desse plano** — construir o gerador +
estratégia de classificação Tier1/2 em escala (fail-safe: default Tier2
pra tudo que não bater em regra clara) + relatório de classificação para
revisão humana **antes** de gerar qualquer `tools/*.go`. Bloqueante: nada
se gera sem esse relatório aprovado, depois um piloto de 1 domínio pequeno
antes de escalar pras 8 fases de domínio.

**O plano anterior (0, 1a, 1, 2, 3, 4 — 29 tools hand-written) está 100%
implementado e verde no vcsim/fixture/host real.** Ver "Done" acima para o
detalhe de cada uma. Plano completo:
`.workspace/plans/MCPVMWare2026-08-09-224451-plano-implementacao-tools-mcp.plan.md`.

**O que falta, e é a única coisa que falta:**
1. **✅ Smoke manual de leitura contra `10.100.2.54` real — feito 10/08/2026
   16:33** (ver "Done" acima): `vmware_about`, `vmware_list_vms/hosts/
   datastores/networks/resource_pools/clusters/datacenters`,
   `vmware_host_info`, `vmware_host_management_ips` — todos confirmados
   contra o host real, dados reais (4 datastores, hardware Dell PowerEdge
   R720, etc.). **Ainda pendente, deliberadamente:** as tools destrutivas
   (`vmware_vm_destroy`, `power_off/reset/suspend`, `snapshot_revert/remove`,
   `vmware_host_maintenance_enter`) — **nunca** sem combinação explícita
   adicional do usuário antes de cada execução real, é o único host/VM de
   teste disponível (`cac-WN02` em `10.100.2.54`, standalone). `.mcp.json`
   do projecto (`mcpVmwareTest`) e o global do Cursor
   (`D:\Users\claiton.linhares\.cursor\mcp.json`, `mcpVmware`) já
   configurados — precisam só reiniciar a respectiva ferramenta pra carregar.
2. **Decisões em aberto do plano** (guest operations/VIX, domínios vistos
   nos MCP concorrentes, backup via VixDiskLib, VMware Cloud on AWS) —
   nenhuma bloqueia nada, todas documentadas em `.workspace/plans/...` §
   "Decisões em aberto", aguardam decisão do usuário se algum round futuro
   fizer sentido.
3. VAMI (Fase 4) continua estruturalmente não-verificável ponta-a-ponta —
   nem o host real (ESXi standalone) nem o vcsim simulam
   `/appliance/system/version`/`/appliance/health/*`; os 4 tools têm teste
   unitário via fixture `httptest`, não integração real.

### Resumo de todas as fases (detalhe completo em "Done" acima)
- **Fase 0** (09/08): sessão/keepalive + harness vcsim.
- **Fase 1a** (10/08): proteção em 3 camadas p/ ações destrutivas.
- **Fase 1** (10/08): `tools/vm.go`, 11 tools de VM lifecycle+snapshots.
- **Fase 2** (10/08): `tools/inventory.go`, 6 tools de listagem + `tools/finder.go`
  (fix multi-datacenter, não previsto originalmente).
- **Fase 3** (10/08): `tools/host.go`, 5 tools de operações de host.
- **Fase 4** (10/08): `tools/appliance.go`, 4 tools starter de VAMI.
- **Total: 28 tools registadas**, confirmadas via smoke end-to-end do
  binário real compilado (`initialize`→`tools/list`→`tools/call`×4 contra
  vcsim através de um subprocess, não só `go test`).

### Closed decisions
*(fechadas com o usuário via AskUserQuestion, 09/08/2026 — ver plano completo)*
- **Escopo:** vSphere on-prem (SOAP/`vim25`, já validado) **+ administração do Appliance (VAMI)**.
- **Domínios desta rodada:** VM lifecycle (power/reconfigure/destroy), Snapshots, Inventário (host/datastore/rede/resource-pool), Host ops (maintenance mode, reconnect, IPs).
- **Sessão de longa duração:** resolver agora (keepalive), é a Phase 0 — fundação, não um "depois". **✅ Implementada 09/08/2026 23:08** (ver "Done" acima), com 1 desvio sinalizado: `session/cache.Session` (interpretação literal da decisão) foi trocado por keepalive em memória — grava tickets em disco tensiona com "nunca persistir credenciais em arquivo"; ver nota no plano §Phase 0.
- **Testes:** ESXi real (`10.100.2.54`) para verificação manual + harness automatizado com `vcsim` (via `referencia/govmomi/simulator` como fonte de leitura, resolvido em produção via `go.mod`) para não depender do host real em toda verificação. **✅ Harness `newSimClient` implementado 09/08/2026 23:08.**
- **Transporte primário do MCP layer:** `govmomi` cobre os dois protocolos numa dependência só — `vim25` (SOAP) **e** `vapi/rest` (REST/VAPI) — não é "govmomi vs REST direto", é "qual(is) subpacote(s) do govmomi usar por domínio". Não existe SDK Go externo melhor (`vsphere-automation-sdk-go` oficial: Beta, sem vSphere/vCenter).
- **VAMI:** parcialmente coberto por `vapi/appliance/{access,logging,networking,shutdown}`; o resto (health/backup/updates/SSH-DCUI/NTP/firewall/contas/serviços) via `vapi/rest` genérico (`Resource(path).Request(method).Do(...)`) — fica pra Phase 4, deliberadamente por último (única fatia não verificável nem no host real nem no vcsim).
- **Proteção contra ações destrutivas (10/08/2026):** 3 camadas — `confirm:bool` estrito por chamada + gate de servidor `--allow-destructive`/`VCENTER_ALLOW_DESTRUCTIVE` (desligado por padrão, bloqueia Tier 1/2 antes de tocar em vCenter, independente do `confirm`) + auditoria local em JSON Lines (`--audit-log-path`, desligada por padrão). Descartada a opção de preview/token de 2 etapas (complexidade extra do lado do cliente MCP). Nova **Fase 1a** no plano (fundação antes da Fase 1, mesmo espírito do keepalive da Fase 0) — classificação por tier: Tier 1 (irreversível) = `vmware_vm_destroy`/`snapshot_remove`/`snapshot_revert`; Tier 2 (disruptivo reversível) = `power_off`/`reset`/`suspend`/`host_maintenance_enter`. Detalhe completo no plano §Fase 1a.

### Open decisions
- ~~VMware Cloud on AWS entra no escopo?~~ — **decidido 10/08/2026 ~19:15** (AskUserQuestion, "Sim, incluir"): entra, como Fase 10 do plano de codegen (99 tools, cliente HTTP próprio, auth CSP token). Ainda **não implementada** (hand-written, sem depender do gerador) — ver "🚀 Next phase". Entrada mantida aqui só até ser removida na próxima limpeza; não é mais uma decisão em aberto.
- **Compatibilidade dos `.mcp.json` existentes:** os 2 ficheiros já em produção usam `--vcenter-url` apontando pra `10.100.2.54` (ESXi standalone, não vCenter) — nome tecnicamente errado sob a semântica dos 5 flags de conexão (implementados 10/08). Não bloqueia nada (o modo `--vcenter-url` continua a funcionar contra ESXi, é só permissivo demais) — decisão de renomear pra `--vmware-url` ou manter fica com o usuário.

---

## 📁 Active architecture

- **Stack:** Go (module `github.com/cslsoftwares/mcpvmware`, Go 1.25.0), código-fonte próprio em `src/` (`mcpvmware-mcp/`, `mcp/`, `tools/`, `vmware/`). Depende de `github.com/vmware/govmomi` via `go.mod` (cache/proxy Go, não lido do clone local). Clone de referência do govmomi (só leitura) em `referencia/govmomi/`.
- **Superfícies de API mapeadas** (fonte → protocolo → cobertura → prioridade p/ MCP):

| Fonte | Protocolo | Cobertura | Prioridade |
|---|---|---|---|
| `referencia/govmomi/` — `session`, `vim25`+`property`, `find`/`list`/`view`, `object`, `task` | SOAP/vim25 | Conexão/sessão, inventário, VM (power/snapshot/clone/reconfigure), host, datastore, rede, polling de tasks | **Alta** — base natural p/ tools MCP |
| `referencia/govmomi/` — `event`, `performance`, `guest`, `nfc`, `alarm` | SOAP/vim25 | Eventos, métricas de performance, operações in-guest, transferência de arquivo (OVF/disco), alarmes | Média |
| `referencia/govmomi/` — `pbm`, `sms`, `vsan`, `vslm`, `cns`, `sts`, `lookup`, `ssoadmin`, `eam`, `crypto` | SOAP/vim25 | Storage policy/VASA, vSAN, FCD, CSI, SSO/identidade, criptografia de VM | Nicho — só se pedido específico surgir |
| `referencia/govmomi/simulator` + `/vcsim` | SOAP mock | vCenter/ESXi simulado em memória, com fault-injection | **Alta p/ testes** — permite testar o MCP sem vCenter real (usado no harness da Phase 0) |
| `referencia/govmomi/vapi/rest` | REST/VAPI (client genérico) | `Resource(path).Request(method).Do(ctx,&out)` — desembrulha `/rest` e `/api` automaticamente; usado pra VAMI (Phase 4) | **Alta p/ VAMI** |
| `.workspace/vSphere Automation REST Resources.postman_collection.json` (~106 requests, 20 pastas) | REST/VAPI | VM lifecycle+hardware, host, datastore, rede, datacenter, resource pool. Auth: Basic→`vmware-api-session-id` ou SAML bearer | Referência — sobrepõe `object`/`vim25` do govmomi em SOAP |
| `.workspace/...appliance.postman_collection.json` (~130 requests, 21 pastas) | REST/VAMI | **Parcialmente coberto** por `vapi/appliance/` (access, logging, networking, shutdown). Falta: health, backup/restore, updates, SSH/DCUI, NTP, firewall, contas locais, serviços — via `vapi/rest` genérico. Auth: Basic | Phase 4 (fatia inicial: version/health/uptime) |
| `.workspace/...Content Library...json` (21 requests, 5 pastas) | REST | Bibliotecas local/subscribed, deploy de OVF. Coberto por `vapi/library/` + `ovf`/`nfc` do govmomi. Auth: Basic | Média |
| `.workspace/VMware Cloud on AWS APIs...json` (~170-200 requests) | REST | **Produto distinto** (VMC on AWS) — fora de escopo salvo pedido explícito | Fora de escopo |
| `.workspace/vSphere Automation REST Samples...json` (34 requests, 7 pastas) | REST | Cookbook de ordem de chamadas | Referência |
| `.workspace/VMWare Automation...json` (3 requests) | REST | Coleção de teste pessoal | Referência mínima |
| `referencia/vmware-esxi-mcp`, `vmware-vcenter-mcp`, `vmware-vsphere-mcp-server` | MCP servers Python (comunidade) | Nenhum resolve VAMI; confirmam padrões (`confirm:bool`, JSON estruturado, PropertyCollector em lote) — ver "Done" 09/08/2026 | Referência de padrão, não dependência |

- **Patterns do MCPVMWare (`src/`):** `mcpvmware-mcp/main.go` (stdio JSON-RPC, flags+env), `mcp/types.go` (protocolo, genérico), `tools/registry.go`+`system.go` (Registry + tools seed), `vmware/client.go` (wrapper govmomi). `confirm:bool` obrigatório em operações destrutivas (padrão dos 3 MCP servers comunidade, adotado). Respostas sempre JSON estruturado (`marshalJSON`), nunca string Markdown solta.

---

## ⚠️ External blockers (don't block coding)

- ~~Senha `root` do ESXi de teste `10.100.2.54`~~ — **resolvido 2026-08-10**, agora em `.mcp.json` (`mcpVmwareTest`, gitignored) além de ter sido fornecida via chat em 09/08. Env vars `VCENTER_URL`/`VCENTER_USERNAME`/`VCENTER_PASSWORD`/`VCENTER_INSECURE=1` continuam suportadas por `src/mcpvmware-mcp` para quem preferir não usar `.mcp.json`.
- ~~Sem rota de rede para `10.100.2.0/24`~~ — **resolvido 2026-08-10 16:33**. Causa real (confirmada pelo usuário, não a hipótese de "ambiente/sessão sem rota" registada mais cedo aqui — corrigido): **o host `10.100.2.54` estava desligado**, não um problema de rede desta sessão (a própria máquina desta sessão está no mesmo /24, `10.100.2.30` — confirmado via `ipconfig`). Usuário religou o host; após ~10min de poll (`curl`/`ping` a cada 20s em background), rede confirmada de volta e smoke real bem-sucedido (ver "Done" acima). Nenhuma acção de rede necessária, só aguardar o boot do ESXi da próxima vez que ele for desligado.
- Como é ESXi standalone (não vCenter), features exclusivas de vCenter (clusters/DRS/vMotion entre hosts, tags/content-library via `vapi`) não se aplicam a este alvo de teste — só o inventário local do próprio host (1 VM encontrada: `cac-WN02`, datastore `6a72b614-...`).

---

## 🔧 Useful commands

```bash
# add the most-used commands here so the next session has them ready
```

---

## 📚 References (read IF needed)

- `.wolf/cerebrum.md` — User Preferences + Do-Not-Repeat + Decision Log
- `.wolf/anatomy.md` — token-efficient file index
- `.wolf/buglog.json` — known bugs + fixes
